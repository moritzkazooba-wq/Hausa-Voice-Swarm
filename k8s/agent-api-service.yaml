apiVersion: v1
kind: Service
metadata:
  name: agent-api
  namespace: hausa-voice-swarm
  labels:
    app: agent-api
    app.kubernetes.io/part-of: hausa-voice-swarm
spec:
  type: ClusterIP
  selector:
    app: agent-api
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
